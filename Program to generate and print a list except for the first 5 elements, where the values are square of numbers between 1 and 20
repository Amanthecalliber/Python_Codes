#Program to generate and print a list except for the first 5 elements, where the values are square of numbers between 1 and 20

li = []

for x in range (1, 21) :
   li.append(x**2)
   y = li[:5]
   z = li[-5:]

a = y + z

print (a)
